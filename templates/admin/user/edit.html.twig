{% extends 'base.html.twig' %}

{% block body %}
    <div class="row mt-5 mb-5 justify-content-center">
        <div class="col-lg-8">
            <div class="d-flex justify-content-center">
                <div style="width: 100%;">
                    <h1 class=" mb-4">{{mode}} utilisateur</h1>
                    {{ include('admin/user/_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
